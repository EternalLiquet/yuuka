name = "yuuka_planner"
description = "Read-only Yuuka planner for implementation-ready plans and acceptance criteria."
sandbox_mode = "read-only"
developer_instructions = """
Invoke the repository's `$yuuka-plan` skill and obey every applicable AGENTS.md.
Do not edit files, commit, push, or open pull requests.
Return the structured plan to the parent agent and stop.
"""
